#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    rows = len(matrix)
    cols = len(matrix[0])
    for i in range(0, cols):
        for r in range(0, rows):
            if r != rows - 1:
                print("{}".format(matrix[r][i]), end=" ")
            if r == rows - 1:
                print("{}".format(matrix[r][i]), end="\n")
