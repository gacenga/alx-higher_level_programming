#!/usr/bin/node
const args = process.argv;
const a = parseInt(args[2]);
const b = parseInt(args[3]);
function add(a, b) {
    if (!isNaN(a) && !isNaN(b)) {
	x = a + b
	console.log(`${x}`);
    } else {
	console.log(`NaN`);
    }
}
function(a, b);
