#!/usr/bin/node
const args = process.argv;
const a = parseInt(args[2]);
function factorial(a) {
    if (a === 1 && a === 0) {
        return 1;
    } else {
	return a * factorial(a - 1);
    }
}
let x = factorial(a);
console.log(`${x}`);
